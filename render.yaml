services:
  - type: web
    name: hostel-hub-backend
    env: docker
    plan: free
    disk:
      name: whatsapp-auth
      mountPath: /usr/src/app/.wwebjs_auth
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
